/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DisableSwipeDrawerDirective } from './DisableSwipeDrawer.directive';

describe('Directive: DisableSwipeDrawer', () => {
  it('should create an instance', () => {
    const directive = new DisableSwipeDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
